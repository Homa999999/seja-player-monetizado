{
  "name": "player-monetizado",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix admin",
    "build:admin": "npm run build --prefix admin",
    "build:assets": "node scripts/minify-assets.mjs",
    "export:content": "node scripts/export-content.mjs",
    "sync:gas": "node scripts/sync-gas-seed.mjs && node scripts/sync-gas-admin-styles.mjs",
    "build:pages": "node scripts/export-content.mjs && npm run build:assets && node scripts/prepare-pages.mjs",
    "dev": "npm run build:admin && npm run dev --prefix server",
    "dev:admin": "npm run dev --prefix admin",
    "start": "npm run build:admin && npm run start --prefix server"
  },
  "devDependencies": {
    "@fontsource/inter": "^5.2.5",
    "@fontsource/sora": "^5.2.5",
    "clean-css": "^5.3.3",
    "terser": "^5.39.0"
  }
}
