# Server
PORT=3000
JWT_SECRET=troque-por-uma-chave-segura

# PostgreSQL / Supabase (opcional — sem isso usa JSON local em /data)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# DATABASE_SSL=true
